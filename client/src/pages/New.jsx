import CreateJob from '../features/jobs/CreateJob';

const New = () => {
  return (
    <section className='flex flex-col items-center'>
      <CreateJob />
    </section>
  );
};

export default New;
