import Application from '../features/jobs/Application';

const Apply = () => {
  return (
    <section className='flex flex-col items-center'>
      <Application />
    </section>
  );
};

export default Apply;
